import { BadgeIconDirective } from './badge-icon.directive';

describe('BadgeIconDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgeIconDirective();
    expect(directive).toBeTruthy();
  });
});
